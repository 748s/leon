{% extends "@Leon/form/element/element.html.twig" %}
{% block content %}
    <input type="password" class="form-control" name="{{ element.name }}" value="{{ form.data[element.name] }}" {% if element.maxLength %}maxlength="{{ element.maxLength }}"{% endif %} {% if element.placeholder %}placeholder="{{ element.placeholder }}" {% endif %} {% if element.minLength %}required {% endif %}/>
{% endblock %}
